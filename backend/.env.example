# Sentinel backend configuration
# Copy this file to .env and adjust values for your environment.

# HTTP server
PORT=3000
ENVIRONMENT=development

# PostgreSQL database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sentinel
DB_USER=sentinel
DB_PASSWORD=changeme

# Authentication (min 32 chars; generate: openssl rand -base64 48)
JWT_SECRET=
